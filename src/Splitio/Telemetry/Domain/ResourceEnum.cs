namespace Splitio.Telemetry.Domain
{
    public enum ResourceEnum
    {
        SplitSync,
        SegmentSync,
        Impressionsync,
        EventSync,
        TelemetrySync,
        TokenSync
    }
}
